name: Build AAB Neo Gridlock

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  android-build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Install Cordova
        run: npm install -g cordova@latest

      - name: Create Project
        run: |
          cordova create build com.neogridlock.myapp "Neo Gridlock"
          cd build
          cordova platform add android@14.0.0
          
          cp ../index.html www/index.html
          cp -r ../assets www/assets || echo "Sem pasta assets"
          
          sed -i 's/version="1.0.0"/version="4.9.5"/' config.xml
          sed -i 's/android-versionCode="1"/android-versionCode="49005"/' config.xml
          
      - name: Configurar XML e Remover Robo
        run: |
          cd build
          
          # Adiciona Logo e SDK 35
          sed -i 's#</widget>#    <icon src="res/icon/icon.png" />\n    <preference name="android-targetSdkVersion" value="35" />\n    <preference name="android-compileSdkVersion" value="35" /></widget>#' config.xml

          mkdir -p res/icon
          cp ../res/icon/icon.png res/icon/icon.png || echo "Sem logo"
          
          echo "=== PACKAGE ==="
          grep "id=" config.xml
          echo "==============="
          
          # PREPARA para gerar os arquivos
          cordova prepare android
          
          # AGORA SIM, EDITA O THEMES.XML
          THEMES="platforms/android/app/src/main/res/values/themes.xml"
          
          # APAGA tudo que é referência a robô/ícone
          sed -i '/windowSplashScreenAnimatedIcon/d' $THEMES
          sed -i '/windowSplashScreenIconBackgroundColor/d' $THEMES
          
          # ✅ CORREÇÃO AQUI: Usa @android:color/black que existe no sistema
          sed -i 's#</style>#<item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item><item name="android:windowSplashScreenBackground">@android:color/black</item></style>#' $THEMES
          
          echo "✅ ROBO REMOVIDO COM SUCESSO"
          grep "windowSplashScreen" $THEMES

          # SUBSTITUI ICONE DO APP
          mkdir -p platforms/android/app/src/main/res/mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}
          for dpi in mdpi hdpi xhdpi xxhdpi xxxhdpi; do
            cp res/icon/icon.png platforms/android/app/src/main/res/mipmap-$dpi/ic_launcher.png || true
          done

      - name: Build
        run: |
          cd build
          cordova build android --release -- --packageType=bundle

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: NeoGridlock-4.8.1-48001-sem-robo
          path: build/platforms/android/app/build/outputs/bundle/release/*.aab
